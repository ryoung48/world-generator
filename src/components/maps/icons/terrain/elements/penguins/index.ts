/* eslint-disable camelcase */
import { IconDef } from '../../../types'
import { penguin_icon } from './types'

const height = 1

export const penguin__icons: Record<penguin_icon, IconDef> = {
  penguin_1: {
    height,
    path: 'terrain/elements/penguins/1.png',
    opacity: 0.8
  }
}
